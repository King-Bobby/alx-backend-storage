-- creates a function SafeDiv
DELIMITER //
CREATE FUNCTION SafeDiv(a INT, b INT)
RETURNS DECIMAL
BEGIN
	IF b = 0 THEN
		RETURN CAST(0 AS DECIMAL);
	ELSE
		RETURN a / CAST(b AS DECIMAL);
	END IF;
END;
//
DELIMITER ;
